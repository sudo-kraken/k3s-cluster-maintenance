---
# Main task file for K3s node maintenance role
- name: Include prerequisite checks
  ansible.builtin.include_tasks: prerequisites.yml
  tags: [always, prerequisites]

- name: Cluster stability check
  delegate_to: localhost
  kubernetes.core.k8s_info:
    api_version: v1
    kind: Node
    wait: true
    wait_condition:
      type: Ready
      status: "True"
    wait_timeout: 120
  tags: [always, stability]

- name: Include package update checks
  ansible.builtin.include_tasks: package_checks.yml
  tags: [packages, check_updates, debian, redhat, reboot, reboot_check]

- name: Include Longhorn health validation (pre-maintenance)
  ansible.builtin.include_tasks: longhorn_validation.yml
  when: k3s_node_maintenance_longhorn_enabled | bool
  tags: [longhorn, health, pre_check]

- name: Include cluster preparation
  ansible.builtin.include_tasks: cluster_preparation.yml
  tags: [cluster, prepare, drain]

- name: Include package updates
  ansible.builtin.include_tasks: package_updates.yml
  tags: [packages, updates, debian, redhat, reboot_check]

- name: Include reboot handling
  ansible.builtin.include_tasks: reboot_handling.yml
  tags: [reboot, reboot_server, reboot_wait, reboot_ready]

- name: Include cluster restoration
  ansible.builtin.include_tasks: cluster_restoration.yml
  tags: [cluster, restore, uncordon]

- name: Include Longhorn health validation (post-maintenance)
  ansible.builtin.include_tasks: longhorn_validation.yml
  when: k3s_node_maintenance_longhorn_enabled | bool
  tags: [longhorn, health, post_check]

- name: Include final cluster validation
  ansible.builtin.include_tasks: final_validation.yml
  tags: [validation, final, cluster_health]

# Resume task for manual recovery after reboot issues
# Only runs when explicitly called with --tags resume
- name: Resume after reboot (manual recovery)
  ansible.builtin.include_tasks: resume_after_reboot.yml
  tags: [resume]
  when: "'resume' in ansible_run_tags"
